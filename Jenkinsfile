pipeline {
    agent any
    stages {
        stage('Init') {
            steps{
              echo "Init stage"
             }
        }

        stage('Deploy') {
            steps{
              echo "Dummy deploy"
             }
        }
    }
}

